iwd1: 1
iwd2: 1
n: 237
name: ReturnToSavedLocation
desc: |-
  This action instructs the active creature to jump to its saved location (as set by SetSavedLocation()).

  ```
  IF
    !NearSavedLocation(7)
  THEN
    RESPONSE #100
      ReturnToSavedLocation()
  END
  ```
